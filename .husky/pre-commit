# Run lint-staged
npx lint-staged

# Check exit code
if [ $? -ne 0 ]; then
  echo "❌ Code quality checks failed. Please fix the issues and try again."
  exit 1
fi

echo "✅ All quality checks passed!"
